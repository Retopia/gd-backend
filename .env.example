# Server Configuration
HOST=0.0.0.0
PORT=8000

# CORS Configuration (comma-separated list of allowed origins, or * for all)
CORS_ORIGINS=*

# Storage Configuration (in MB)
STORAGE_LIMIT_MB=500

# File Upload Limits
MAX_FILE_SIZE_MB=100
